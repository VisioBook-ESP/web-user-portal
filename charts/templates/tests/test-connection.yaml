apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-user-portal.fullname" . }}-test-connection"
  labels:
    {{- include "web-user-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-user-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
